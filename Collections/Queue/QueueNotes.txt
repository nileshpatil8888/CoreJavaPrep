Queue :
       A collections used to hold the elements prior to processing.
       It follows to FIFO (First In First Out) principle.
       It can have duplicates.
       Common Implementations : :LinkedList, PriorityQueue, ArrayDeque
       Methods : offer(), poll(), peek() etc.
